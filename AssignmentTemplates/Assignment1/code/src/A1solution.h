#pragma once
#include <string>

class A1solution {
public:
    A1solution();
    void run(const std::string& filename);
};

/**
 * 
 * Create a class A1solution
 * Read a file containing: Matrices, Window size, Vertices, Triangles
 * Open an OpenGL window
 * Render the model
 * Implement 4 shaders
 * Toggle between them when pressing 's'
 * 
 */