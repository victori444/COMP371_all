/*
The entry point to your code is a file src/A1solution.h that implements a A1solution class.

a. The class must have a constructor that takes one string as input and this string is the name of a
file from the disk that contains all the information you needed.

b. It has a run() member function that is called from the main function and should do the
following:
    i. Open an OpenGL window (similarly with the capsules provided)
    ii. Loads and Renders a 3D model in an interactive loop
    iii. Listens to the keyboard and upon oppressing ‘s’ it will toggle between 4 different
    rendering modes and pressing ‘w’ will toggle between the regular shading and a
    wireframe mode. 
*/